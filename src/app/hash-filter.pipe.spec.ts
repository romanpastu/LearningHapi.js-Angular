import { HashFilterPipe } from './hash-filter.pipe';

describe('HashFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HashFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
